import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from './ui/card';

export const AMR_Human_Overview_Heat_Test = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>AMR Heatmap Test</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Test component loading...</p>
      </CardContent>
    </Card>
  );
};